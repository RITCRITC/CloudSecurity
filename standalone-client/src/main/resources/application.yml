spring:
  datasource:
    name: client-db
    username: client-user
    # provide sample-password as jasypt.encryptor.password to decrypt
    password: ENC(${client.encrypted-database-password})
  h2:
    console:
      enabled: true

client:
  name: standalone-client
  encrypted-database-password: byF912frlZUCifdrOFlGJ8LTjWpVIutKtzU0St2X/hZ9sqRp9kOsg5Se8FVxshFu

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: info
